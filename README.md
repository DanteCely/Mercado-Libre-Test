# Mercado-Libre-Test

To run the test you have to fisrt run the back end project and then the fronend project.

### Backend project

`npm run start:dev`

### Frontend project

run the code
`npm start`
or
`yarn start`
